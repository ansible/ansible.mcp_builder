---
# Common role tasks - Set up generic MCP build environment

# ---------------- Go setup tasks ----------------
- name: Discover all MCP server registries from roles
  ansible.builtin.set_fact:
    common_discovered_registries: >-
      {%- set registries = [] -%}
      {%- for var_name in vars.keys() -%}
        {%- if var_name.endswith('_mcp_registry') -%}
          {%- set var_value = vars[var_name] -%}
          {%- if var_value is iterable and var_value is not string -%}
            {%- for server in var_value -%}
              {%- if server is mapping and server.name is defined and server.type is defined -%}
                {%- set _ = registries.append(server) -%}
              {%- endif -%}
            {%- endfor -%}
          {%- endif -%}
        {%- endif -%}
      {%- endfor -%}
      {{ registries }}

- name: Set install flags based on discovered registry entries
  ansible.builtin.set_fact:
    common_install_go: "{{ common_discovered_registries | selectattr('type', 'equalto', 'go') | list | length > 0 }}"
    common_npm_install: "{{ common_discovered_registries | selectattr('type', 'equalto', 'npm') | list | length > 0 }}"
    common_pypi_install: "{{ common_discovered_registries | selectattr('type', 'equalto', 'pypi') | list | length > 0 }}"

- name: Include Go installation tasks
  ansible.builtin.include_tasks: go_install.yml
  when: common_install_go

# ---------------- Node setup tasks ----------------
- name: Include npm installation tasks
  ansible.builtin.include_tasks: npm_install.yml
  when: common_npm_install

# ---------------- PyPI setup tasks ----------------
- name: Include PyPI installation tasks
  ansible.builtin.include_tasks: pypi_install.yml
  when: common_pypi_install

- name: Install jq for JSON processing
  ansible.builtin.package:
    name: jq
    state: present
  become: true

- name: Create unified MCP bin directory
  ansible.builtin.file:
    path: "{{ common_mcp_base_path }}/bin"
    state: directory
    mode: "0755"
  become: true

- name: Create MCP management script
  ansible.builtin.template:
    src: mcp_manage.j2
    dest: "{{ common_mcp_base_path }}/mcp_manage"
    mode: "0755"

- name: Create symlink for mcp_manage in /usr/local/bin
  ansible.builtin.file:
    src: "{{ common_mcp_base_path }}/mcp_manage"
    dest: /usr/local/bin/mcp_manage
    state: link
    force: true
  become: true

- name: Assign paths to all MCP servers (centralized)
  ansible.builtin.include_tasks: assign_registry_paths.yml

- name: Generate MCP servers manifest
  ansible.builtin.include_tasks: generate_manifest.yml

- name: Create symlinks for Go binaries in /usr/local/bin
  ansible.builtin.file:
    src: "{{ item.path }}"
    dest: "/usr/local/bin/{{ item.name }}"
    state: link
    force: true
  become: true
  loop: "{{ common_discovered_registries | selectattr('type', 'equalto', 'go') | list }}"
  when: common_discovered_registries | selectattr('type', 'equalto', 'go') | list | length > 0

- name: Create wrapper scripts for npm servers in /usr/local/bin
  ansible.builtin.template:
    src: mcp_wrapper.j2
    dest: "/usr/local/bin/{{ item.name }}"
    mode: "0755"
  become: true
  vars:
    server_name: "{{ item.name }}"
    server_type: "{{ item.type }}"
  loop: "{{ common_discovered_registries | selectattr('type', 'equalto', 'npm') | list }}"
  when: common_discovered_registries | selectattr('type', 'equalto', 'npm') | list | length > 0

- name: Create wrapper scripts for pypi servers in /usr/local/bin
  ansible.builtin.template:
    src: mcp_wrapper.j2
    dest: "/usr/local/bin/{{ item.name }}"
    mode: "0755"
  become: true
  vars:
    server_name: "{{ item.name }}"
    server_type: "{{ item.type }}"
  loop: "{{ common_discovered_registries | selectattr('type', 'equalto', 'pypi') | list }}"
  when: common_discovered_registries | selectattr('type', 'equalto', 'pypi') | list | length > 0

- name: Display MCP servers usage info
  ansible.builtin.debug:
    msg: "MCP servers are available globally by name (e.g., 'github-mcp-server', 'mcp-hello-world') and via 'mcp_manage run <server>'."
